<li>

   <%= f.radio_button :category_id ,category.id%> <%= category.name %> - <b> <%= "$ #{category.lowest_number_of_points_needed}" %> </b>
   <span></span>
   <% unless category.children.empty? %>
	    <ul>
	        <%= render :partial => 'category_list', :collection => category.children, :as => :category ,:locals => { :f => f} %>
	    </ul>
	 <%end%>
</li>

