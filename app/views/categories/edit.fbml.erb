
 <% facebook_form_for :wish_list, @wish_list, :action => "update_wishlist" ,:url => update_wishlist_wish_lists_path(:wish_list => @wish_list.id), :method => :get do |f| %>
<div class="main">
<div class="container">
	<div id = 'category-list'>
   		<%= render :partial => 'list' %>
	</div>
</div>

<div class="sidebar">
   <h2>Category list</h2>
	<ul id='tree'><%= render :partial => 'category_list', :collection => @parent, :as => :category %></ul>
</div>
</div>
<div class="clear"></div>
<%= f.buttons "save to wishlist" %>
 
<% end %>
